# Agent: plan-checker-dim-9-stale-line-anchor

Thin wrapper for Dim 9 (stale-line-anchor) invoked by `build-plan` Step 6.

Skill(skill="plan-checker-dim-9-stale-line-anchor", args={plan_text, entity_context})

Returns YAML issues only. Parser expects format from SKILL.md.
