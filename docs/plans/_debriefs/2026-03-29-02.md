---
session-date: 2026-03-29
sequence: 2
first-commit: 940cdab
last-commit: 124359e
duration: ~5h
---

# Session Debrief — 2026-03-29 #2

## Work Completed

- 059 guardrail-issue-first-workflow — PR #12 merged (detected at startup)
- 068 keep-implementer-alive — PR #13 merged (detected at startup)
- 071 pr-merge-detection-in-session — PR #14 merged, archived
- 072 fo-cwd-drift-bug — local merge, archived. FO template now prohibits `cd` into worktrees.
- 073 auto-reject-feedback-stages — local merge, archived. Feedback rejections auto-bounce without captain wait.
- 074 fo-startup-improvements — PR #16 merged. Grep exclusions, bare mode detection, branch constraint.
- 077 merge-hook-guardrail — PR #15 merged. Single execution point for merge hooks.
- 078 modular-test-harness — PR #17 merged. All 7 test scripts rewritten from bash to Python+uv. Shared test_lib, snapshot mechanism, auto stats.

## Commits

74 commits across workflow directory. Key groups:
- 033 graceful-degradation: experiment design, two experiments, validation cycles
- 072 cwd drift: filed → ideation → implementation → validation → merged (single session)
- 073 auto-reject: filed → ideation → implementation → validation → merged (single session)
- 074 startup: ideation → implementation → validation → PR → merged
- 077 merge hook: filed → ideation → implementation → validation → PR → merged
- 078 test harness: filed → ideation → implementation → validation → fix cycles → PR → merged

## Decisions

- **Disable teams vs template rewrite**: CL directed 033 as an experiment (test what breaks) rather than a template rewrite. Experiment confirmed FO degrades gracefully — ToolSearch probe discovers missing tools.
- **Auto-reject at feedback gates**: Validator REJECTED → auto-bounce to implementer without captain. Captain only needed for approvals. (073)
- **Bare mode**: FO detects TeamCreate absence via ToolSearch, reports "bare mode" to captain, continues with sequential dispatch. (074)
- **Plugin-shipped agents**: Architectural decision to ship FO/ensign with the plugin (like superpowers:code-reviewer). Eject skill for version pinning. Commission simplified. (076 in design)
- **Python+uv for tests**: Redirected 078 from bash test-lib.sh to Python mid-implementation. Inline python in bash was a code smell.
- **Opus/low as test default**: Model variation matrix showed opus/low is the sweet spot (128s, 8/8 PASS). Haiku/sonnet hit $2 budget cap.
- **Staff review at ideation gate**: For complex tasks (score >= 0.8, scaffolding changes, E2E tests), FO spawns a fresh reviewer subagent before presenting at gate. Workflow README updated.

## Issues — Workflow

- FO cwd drift into worktrees caused agent to write to wrong directory (072, fixed)
- FO skipped merge hooks on 073 approval — went straight to local merge (077, fixed)
- FO waited for captain on clear validator rejection (073, fixed with auto-bounce)
- Ensign committed test script change to main from worktree (074 branch constraint, fixed)
- Checklist E2E test had 2 regressions from template wording drift (fixed, restored inline DONE/SKIPPED/FAILED)
- Gate guardrail test had 3 pre-existing failures from template text drift (fixed in 078)
- 033 validator hit context limit after extended captain-driven testing

## Issues — Spacedock

- `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=0` has no effect — env var doesn't disable team tools. Use `--disallowed-tools` instead.
- Agent types frozen at session init — dynamically-created agents unavailable mid-session. Affects commission auto-run.
- `fresh` field in stage definitions is dead in 0.8.2 template — always dispatches fresh. Regression from 0.3.0. (075 filed)
- Keep-alive behavior (068) not available to current session FO — agent loaded before merge. Version staleness problem that 076 would solve.

## Observations

- **Workflow overhead vs value**: The full pipeline (ideation → implementation → validation) is over-heavy for trivial template wording changes (072 was 6 lines). Valuable for complex work (033 experiment, 078 rewrite). Need per-task rigor control.
- **Staff review works**: Caught real gaps in 077 (test plan insufficient, wording precision) and 078 (snapshot scope overstated, commission prompt difference). Worth the subagent cost for complex tasks.
- **Model variation findings**: Opus/low is fastest-passing (128s). Haiku is fastest overall (57s) but hits budget cap. Sonnet similar to haiku in messages but slower wallclock.
- **Feedback cycles work**: 033 got rejected by validator, auto-bounced to implementer, fixed, re-validated, passed. 078 had similar cycle for argparse bug. The rejection flow is functional.

## What's Next

- 033 graceful-degradation — validation PASSED, awaiting merge approval
- 076 plugin-shipped-agents — ideation complete with staff review. 3 blocking items to verify before implementation (agent resolution order, Phase 3 auto-run, local vs plugin precedence)
- 075 restore-ensign-reuse — backlog (fresh field regression)
- 036 commission-compile-targets — backlog
- 058 terminology-experiment — orphan in validation, worktree exists, no PR
- 057, 031, 054 — parked ideation
